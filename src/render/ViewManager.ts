import{EventBus}from'../core/EventBus';import type{StateManager}from'../core/StateManager';export type GameView='dungeon'|'town'|'heroes'|'blacksmith'|'raid';export class ViewManager{private buttons=new Map<GameView,HTMLButtonElement>();constructor(private state:StateManager,private bus:EventBus,private root:HTMLElement){}mount(){const nav=document.createElement('nav');nav.className='bottom-nav';const entries:Array<[GameView,string,string]>=[['dungeon','Dungeon','⚔'],['town','Town','⌂'],['heroes','Heroes','♟'],['blacksmith','Forge','⚒'],['raid','Raid','♜']];for(const[view,label,icon]of entries){const b=document.createElement('button');b.innerHTML=`<span class="nav-icon">${icon}</span><span>${label}</span>`;b.addEventListener('click',()=>this.show(view));nav.appendChild(b);this.buttons.set(view,b);}this.root.appendChild(nav);const params=new URLSearchParams(location.search);const forcedTown=params.get('forcetown')==='1';if(forcedTown){const badge=document.createElement('div');badge.textContent='STORY HUB BUILD · TOWN FORCED';badge.style.cssText='position:fixed;z-index:99;top:calc(env(safe-area-inset-top) + 4px);right:8px;padding:4px 7px;border:1px solid #d4a75c;background:#17110eee;color:#f0d7a5;font:700 9px Georgia,serif;letter-spacing:.6px;border-radius:5px;pointer-events:none';this.root.appendChild(badge);}this.show(forcedTown?'town':((this.state.activeView as GameView)||'town'));}show(view:GameView){this.state.activeView=view;this.root.dataset.view=view;document.querySelectorAll<HTMLElement>('[data-view]').forEach(p=>p.hidden=p.dataset.view!==view);this.buttons.forEach((b,k)=>b.classList.toggle('active',k===view));this.bus.emit('view:change',{view});this.state.save();}}
